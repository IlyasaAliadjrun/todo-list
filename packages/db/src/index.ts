/**
 * @notion/db — Prisma client & schema. Satu-satunya jalur ke database.
 * Impor `PrismaClient` (dan tipe model) dari "@notion/db".
 */
export * from "@prisma/client";
export { PrismaClient } from "@prisma/client";
